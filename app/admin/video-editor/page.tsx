import VideoEditorClient from "@/components/admin/video-editor/VideoEditorClient";

export const metadata = {
  title: "Video Editor — Astropsyche Admin",
};

export default function VideoEditorPage() {
  return <VideoEditorClient />;
}
